# User Input Drop Zone

Put your source files here:

- `audio/`: one MP3 file (example: 5 minutes)
- `images/`: 5 images (`.jpg`, `.jpeg`, `.png`)
- `output/`: generated long video files

Recommended image resolution: 1920x1080 (or larger, same aspect ratio).
