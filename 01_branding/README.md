# Branding Assets

Store all brand identity materials here.

- `logos/` for transparent PNG/SVG logos
- `channel-art/` for YouTube banner and profile variants
- `thumbnails/` for reusable stream thumbnail templates
